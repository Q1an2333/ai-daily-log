# 2026-04-11 Daily Memory

## AGENTS.md 更新 - 连续失败熔断机制
- 新增全局操作逻辑：「弹性步数、严格重试」
- 熔断触发条件：连续 5 次失败 → 立即终止任务
- 重置触发器：任何操作成功 → 计数器清零
- 熔断报告格式：「任务因在 [步骤名] 连续重复失败 [N] 次而触发熔断，已停止以节省资源」

## 知乎浏览器自动化尝试
- 用户目标：用本地 Edge Profile 的登录态回答知乎问题
- 尝试方案：
  1. ❌ Edge Profile 路径桥接（/mnt/c/...）：WSL + Edge Pipe 方式不兼容
  2. ❌ Linux 原生持久化（/home/qian/zhihu_wsl_profile）：知乎检测自动化指纹，重定向回首页
  3. ⚠️ 登录态 Cookie 有效（z_c0 存在，187字符），但浏览器访问被阻止
- 根本原因：知乎检测 Playwright/Chromium 自动化特征，强制重定向
- 待解决：方案1（提取 Cookie 用 API 提交）/ 方案2（Puppeteer stealth 插件）
- Profile 目录：/home/qian/zhihu_wsl_profile（新建 Linux 原生独立目录）
- 目标问题：https://www.zhihu.com/question/661365697（AI 是什么？未来会发展成什么样？普通人应该如何适应？）
- 预设回答内容：已准备好一段关于 AI 发展的回答（4个要点，约400字）

## 小红书（XHS）自动化阵地开辟
- **目标**：用 Windows Chrome 已登录态，在 Linux Playwright 中完成小红书评论操作
- **环境**：WSL2，目录 `/home/qian/xhs_bot_context`
- **架构**：通过 CDP（Chrome DevTools Protocol）连接用户已登录的 Windows Chrome 浏览器
- **CDP 连接方式**：
  - Windows Chrome 启动参数：`--new-window --no-sandbox --remote-debugging-port=9222`
  - 监听地址：`ws://127.0.0.1:9222/devtools/browser/<browser-id>`
  - **重要**：`localhost` 在 WSL2 中解析有问题，必须用 `127.0.0.1`
- **CDP 连接成功**：
  - Windows Chrome 标签页 ID：`8818094B2B231587C8CF164653F9C688`
  - 页面 URL：`https://www.xiaohongshu.com/explore`
  - 成功提取 16 个 XHS Cookie（acw_tc, a1, webId, web_session, id_token 等）
  - ❌ Cookie 无法直接跨平台迁移（httpOnly + Windows DPAPI 加密）
- **Playwright CDP 连接**：
  - 使用 `chromium.connectOverCDP('ws://127.0.0.1:9222/...')` 连接已有浏览器
  - 获取已有页面：`browser.contexts()[0].pages()` → 找到小红书标签页
- **评论输入修复**：
  - contenteditable 输入框被 `.not-active.inner-when-not-active` 遮罩层挡住
  - `click()` 需要加 `{ force: true }` 绕过拦截
  - `execCommand('insertText')` 对 contenteditable 有效
  - 成功点击「发送」按钮
- **待确认**：评论是否真正发出（刷新页面后未找到评论文本，可能是时间差问题）
- **启动脚本**：
  ```bash
  # Windows 侧启动 Chrome
  cmd.exe /c "start \"\" \"C:\Users\Q1an\AppData\Local\Google\Chrome\Application\chrome.exe\" --new-window --no-sandbox --remote-debugging-port=9222"
  ```

## 小红书评论自动化 - 最终方案

### 核心架构
1. **Windows Chrome** 通过 `--remote-debugging-port=9222` 启动，用户扫码登录
2. **Linux Playwright** 通过 CDP (`ws://127.0.0.1:9222/devtools/browser/<id>`) 连接已有 Chrome
3. **智谱 GLM-4-Flash** 生成 contextual 评论（30字以内）

### 关键文件
- `/home/qian/xhs_bot_context/xhs_llm_comment.js` — 主脚本（随机选文+LLM生成+评论）
- `/home/qian/xhs_bot_context/cdp_get_cookies.js` — 从 CDP 获取 Cookie（备用）
- `/home/qian/xhs_bot_context/check_weather.sh` — 天气检查脚本参考

### 启动流程
```bash
# 1. Windows 侧启动 Chrome
cmd.exe /c "start \"\" \"C:\Users\Q1an\AppData\Local\Google\Chrome\Application\chrome.exe\" --new-window --no-sandbox --remote-debugging-port=9222"

# 2. 确认 CDP 可用
curl -s http://127.0.0.1:9222/json | grep xiaohongshu

# 3. 运行评论脚本
cd /home/qian/xhs_bot_context && node xhs_llm_comment.js
```

### 评论输入关键点
- contenteditable 输入框被遮罩层 `.not-active.inner-when-not-active` 挡住
- 需要 `{ force: true }` 点击 `#content-textarea`
- 使用 `document.execCommand('insertText')` 输入文字
- 提交按钮文字可能是「发布」「发送」「评论」

### LLM 评论生成（智谱 GLM-4-Flash）
- API: `https://open.bigmodel.cn/api/paas/v4/chat/completions`
- Key: `384bb3f54a9b444a83d5e5b08799cbca.r3kRQmCZQF9QgZyj`
- Model: `glm-4-flash`
- Prompt: 根据文章标题和内容生成30字内小红书风格评论

### 验证成功的评论示例
| 文章 | 评论 |
|------|------|
| 小眼睛逆袭攻略 | 「小眼睛也能成博主王炸！🌟💫 #小眼睛逆袭#」 |
| 我爱杰伦 | 「杰伦，你的音乐陪伴我成长，永远爱你！🎶💖」 |
| 仰卧起坐vs引体向上 | 「仰卧起坐更轻松，选择适合自己的吧！🏃♂️💪」 |

## 技术笔记
- `chromium.launchPersistentContext` 的 `profile` 参数在某些版本不支持
- `--remote-debugging-pipe` 在 WSL2 + Windows Edge 环境下不兼容
- SingletonLock 文件会阻止重复启动，需要先删除
- 知乎登录 Cookie：`z_c0` 是关键登录凭证
